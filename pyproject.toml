[project]
name = "prefect-superset"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.2",
    "prefect>=3.4",
]

[dependency-groups]
dev = [
    "pandas-stubs>=2.2",
    "ruff>=0.11",
]
